
class TrackingCodeProcessor {

  /**
   * Given a set of tracking codes, will return a summarized view of how long 
   * it takes to delivery a package under 3, 2, 1 days.
   * @param trackingCodes 
   */
  public processTrackingCodes(destinationZip: string, days: number, trackingCodes: string[]): ProcessorResult[] {
    return [];
  }
}